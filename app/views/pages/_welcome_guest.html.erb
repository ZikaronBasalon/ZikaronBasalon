<h3><%= t '.title' %></h3>
<p class="subtitle">
  <div class="col-xs-8 col-xs-offset-2">
    <%= simple_format(t('.subtitle', host_link: link_to(t('.host_link_text'), signup_path(type: 'host')), guest_link: link_to(t('.guest_link_text'), signup_path(type: 'guest')))) %>
  </div>
</p>